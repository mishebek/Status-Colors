Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/bleeding.png';
  CONFIG.statusEffects.find(s => s.id === 'bleeding').img = img;
  CONFIG.DND5E.conditionTypes.bleeding.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/blinded.png';
  CONFIG.statusEffects.find(s => s.id === 'blinded').img = img;
  CONFIG.DND5E.conditionTypes.blinded.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/burning.png';
  CONFIG.statusEffects.find(s => s.id === 'burning').img = img;
  CONFIG.DND5E.conditionTypes.burning.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/burrowing.png';
  CONFIG.statusEffects.find(s => s.id === 'burrowing').img = img;
  CONFIG.DND5E.conditionTypes.burrowing.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/charmed.png';
  CONFIG.statusEffects.find(s => s.id === 'charmed').img = img;
  CONFIG.DND5E.conditionTypes.charmed.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/cursed.png';
  CONFIG.statusEffects.find(s => s.id === 'cursed').img = img;
  CONFIG.DND5E.conditionTypes.cursed.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/deafened.png';
  CONFIG.statusEffects.find(s => s.id === 'deafened').img = img;
  CONFIG.DND5E.conditionTypes.deafened.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/dehydration.png';
  CONFIG.statusEffects.find(s => s.id === 'dehydration').img = img;
  CONFIG.DND5E.conditionTypes.dehydration.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/diseased.png';
  CONFIG.statusEffects.find(s => s.id === 'diseased').img = img;
  CONFIG.DND5E.conditionTypes.diseased.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/exhaustion.png';
  CONFIG.statusEffects.find(s => s.id === 'exhaustion').img = img;
  CONFIG.DND5E.conditionTypes.exhaustion.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/falling.png';
  CONFIG.statusEffects.find(s => s.id === 'falling').img = img;
  CONFIG.DND5E.conditionTypes.falling.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/frightened.png';
  CONFIG.statusEffects.find(s => s.id === 'frightened').img = img;
  CONFIG.DND5E.conditionTypes.frightened.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/grappled.png';
  CONFIG.statusEffects.find(s => s.id === 'grappled').img = img;
  CONFIG.DND5E.conditionTypes.grappled.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/incapacitated.png';
  CONFIG.statusEffects.find(s => s.id === 'incapacitated').img = img;
  CONFIG.DND5E.conditionTypes.incapacitated.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/invisible.png';
  CONFIG.statusEffects.find(s => s.id === 'invisible').img = img;
  CONFIG.DND5E.conditionTypes.invisible.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/malnutrition.png';
  CONFIG.statusEffects.find(s => s.id === 'malnutrition').img = img;
  CONFIG.DND5E.conditionTypes.malnutrition.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/paralyzed.png';
  CONFIG.statusEffects.find(s => s.id === 'paralyzed').img = img;
  CONFIG.DND5E.conditionTypes.paralyzed.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/petrified.png';
  CONFIG.statusEffects.find(s => s.id === 'petrified').img = img;
  CONFIG.DND5E.conditionTypes.petrified.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/poisoned.png';
  CONFIG.statusEffects.find(s => s.id === 'poisoned').img = img;
  CONFIG.DND5E.conditionTypes.poisoned.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/prone.png';
  CONFIG.statusEffects.find(s => s.id === 'prone').img = img;
  CONFIG.DND5E.conditionTypes.prone.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/restrained.png';
  CONFIG.statusEffects.find(s => s.id === 'restrained').img = img;
  CONFIG.DND5E.conditionTypes.restrained.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/silenced.png';
  CONFIG.statusEffects.find(s => s.id === 'silenced').img = img;
  CONFIG.DND5E.conditionTypes.silenced.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/stunned.png';
  CONFIG.statusEffects.find(s => s.id === 'stunned').img = img;
  CONFIG.DND5E.conditionTypes.stunned.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/suffocation.png';
  CONFIG.statusEffects.find(s => s.id === 'suffocation').img = img;
  CONFIG.DND5E.conditionTypes.suffocation.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/surprised.png';
  CONFIG.statusEffects.find(s => s.id === 'surprised').img = img;
  CONFIG.DND5E.conditionTypes.surprised.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/transformed.png';
  CONFIG.statusEffects.find(s => s.id === 'transformed').img = img;
  CONFIG.DND5E.conditionTypes.transformed.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/unconscious.png';
  CONFIG.statusEffects.find(s => s.id === 'unconscious').img = img;
  CONFIG.DND5E.conditionTypes.unconscious.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/cover-half.png';
  CONFIG.statusEffects.find(s => s.id === 'coverHalf').img = img;
  CONFIG.DND5E.conditionTypes.coverHalf.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/cover-three-quarters.png';
  CONFIG.statusEffects.find(s => s.id === 'coverThreeQuarters').img = img;
  CONFIG.DND5E.conditionTypes.coverThreeQuarters.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/cover-total.png';
  CONFIG.statusEffects.find(s => s.id === 'coverTotal').img = img;
  CONFIG.DND5E.conditionTypes.coverTotal.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/concentrating.png';
  CONFIG.statusEffects.find(s => s.id === 'concentrating').img = img;
  CONFIG.DND5E.conditionTypes.concentrating.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/dodging.png';
  CONFIG.statusEffects.find(s => s.id === 'dodging').img = img;
  CONFIG.DND5E.conditionTypes.dodging.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/flying.png';
  CONFIG.statusEffects.find(s => s.id === 'flying').img = img;
  CONFIG.DND5E.conditionTypes.flying.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/hiding.png';
  CONFIG.statusEffects.find(s => s.id === 'hiding').img = img;
  CONFIG.DND5E.conditionTypes.hiding.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/hovering.png';
  CONFIG.statusEffects.find(s => s.id === 'hovering').img = img;
  CONFIG.DND5E.conditionTypes.hovering.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/marked.png';
  CONFIG.statusEffects.find(s => s.id === 'marked').img = img;
  CONFIG.DND5E.conditionTypes.marked.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/sleeping.png';
  CONFIG.statusEffects.find(s => s.id === 'sleeping').img = img;
  CONFIG.DND5E.conditionTypes.sleeping.icon = img;
});

Hooks.once("init", () => {
  const img = 'modules/status-colors/statusicons/stable.png';
  CONFIG.statusEffects.find(s => s.id === 'stable').img = img;
  CONFIG.DND5E.conditionTypes.stable.icon = img;
});